name: rtl

commands:
  - |
    mkdir -p outputs
    cp -r $VLOG_HOME/digital_top ./outputs
    cat "./outputs/digital_top/digital_top.sv" > "./outputs/design.v"
    python gen_tcl.py


outputs:
  - design.v
  - read_design.tcl
